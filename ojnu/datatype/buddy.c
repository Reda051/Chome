#include <stdio.h>
/*这是指针和链表的经典应用，也是现实世界中真实存在的内存分配算法，但大家不需要去学习额外的知识，我们希望大家使用数组模拟实现。
这一次先体验数组的实现方式，等到学习了指针和链表之后再回过头看，会有更深的理解。
题面的描述均采用偏向于链表的理解方式，在本周目中大家可能会对一知半解，类比生活中的“链”，现在只需知道“链表”是由一些节点（物品）依次连接形成的一种线状结构即可。
时限已经适当放宽（标程的三倍以上），但太劣的实现应当仍然无法通过，请大家注意效率。*/
int main(void){






    return 0;
}